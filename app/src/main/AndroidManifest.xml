<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whowantstobemillionaire_yadavl1">

    <application
        android:allowBackup="true"
        android:configChanges="orientation|screenSize|keyboardHidden"
        android:icon="@mipmap/wwtbam"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wwtbam"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhoWantsTobeMillionaire">

        <activity android:name="com.example.whowantstobemillionaire_yadavl1.QuizResultActivity"
            android:exported="true" />

        <activity android:name="com.example.whowantstobemillionaire_yadavl1.QuizStartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
